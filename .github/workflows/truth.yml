name: Truth

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  truth:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: 22
      - run: node scripts/check-public-truth.mjs
